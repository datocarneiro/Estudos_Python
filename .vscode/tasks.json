{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clear Terminal",
            "type": "shell",
            "command": "clear",
            "problemMatcher": [],
            "presentation": {
                "reveal": "silent", // Modo de exibição da saída do comando (silencioso)
                // "panel": "shared", 
                "panel": "dedicated"  // Usar painel compartilhado para a saída
            }
        }
    ]
}
